server:
  #运行端口号
  port: 8101
spring:
  application:
    #服务名称
    name: eureka-client
eureka:
  client:
    #获取注册实例列表
    fetch-registry: true
    #注册到Eureka的注册中心
    register-with-eureka: true
    service-url:
      #配置注册中心地址
      defaultZone: http://localhost:8001/eureka/
