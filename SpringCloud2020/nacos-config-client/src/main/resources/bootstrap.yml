server:
  port: 8907
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        #Nacos地址
        server-addr: localhost:8848
      config:
        #Nacos地址
        server-addr: localhost:8848
        #这里我们获取的yaml格式的配置
        file-extension: yaml
